const Input = () => {
  return (
    <div className="input__wrapper">
      <input
        className="input"
        type="text"
        placeholder="What needs to be done?"
      />
    </div>
  );
};

export default Input;
